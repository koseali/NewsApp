# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppcentApp' do
  pod 'Defaults'
  pod 'Kingfisher', '~> 6.0'
  pod 'SVProgressHUD'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppcentApp
  target 'AppcentAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppcentAppUITests' do
    # Pods for testing
  end

end
